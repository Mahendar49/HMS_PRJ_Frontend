import React from 'react';import sample from '../data.sample.json';export default function Appointments(){return (<div><h3>Appointments</h3><table className='table'><thead><tr><th>#</th><th>Patient</th><th>When</th></tr></thead><tbody>{sample.appointments.map(a=>{const p=sample.patients.find(x=>x.id===a.patient_id);return <tr key={a.id}><td>{a.id}</td><td>{p?.first_name} {p?.last_name}</td><td>{new Date(a.scheduled_start).toLocaleString()}</td></tr>})}</tbody></table></div>)}